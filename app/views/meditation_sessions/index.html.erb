<h1 class="app-title">Meditation.io</h1>

<%= render "registration" %>

<% if @user %>

  <table id="sessions">
    <thead>
      <th>Date</th>
      <th>Minutes</th>
      <th>Comments</th>
      <th></th>
    </thead>
    <tbody>
    <%= render @meditation_session %>
    <tbody>
  </table>

  <%= button_to 'Meditate', new_meditation_session_path, method: :get, id: "new_session" %>


<% end %>
